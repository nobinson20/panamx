int main() {
    matrix m;
    m = [1, 2, 3;
         4, 5, 6;
         7, 8, 9];
    printf(sum(m));
    printf(mean(m));
    printm(trans(m));
    printm(m);
    printm(rref(m));
    printf(rank(m));
    return 0;
}
